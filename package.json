{
  "name": "@wrikka/tsdown-plugins",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@3.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "test": "turbo test",
    "release": "turbo release",
    "update:deps": "taze -r -w && ni",
    "review": "bun format && bun lint && bun test && bun update:deps"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.2",
    "release-it": "^19.0.5",
    "rolldown": "^1.0.0-beta.45",
    "taze": "latest",
    "tsdown": "^0.15.12",
    "turbo": "latest",
    "vitest": "^4.0.6"
  }
}
